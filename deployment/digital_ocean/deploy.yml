---

- include: ../common/deploy.yml
  vars:
    master_hosts_group: "master"
    worker_hosts_group: "workers"
    preferred_network_interface: "eth1"
    preferred_ansible_interface: "ansible_default_ipv4"

